# 启动脚本管理功能 - 测试计划

## 测试日期
2026-03-15

## 测试范围

### 后端 API 测试

#### 1. 脚本管理 API
- [ ] `GET /api/service-start-script/list` - 获取所有脚本列表
- [ ] `GET /api/service-start-script/{id}` - 获取脚本详情
- [ ] `POST /api/service-start-script/create` - 创建新脚本
- [ ] `POST /api/service-start-script/update` - 更新脚本
- [ ] `POST /api/service-start-script/delete/{id}` - 删除脚本
- [ ] `GET /api/service-start-script/{scriptId}/versions` - 获取版本列表
- [ ] `POST /api/service-start-script/{scriptId}/activate/{versionId}` - 激活版本
- [ ] `GET /api/service-start-script/default` - 获取默认脚本

#### 2. 部署集成测试
- [ ] 验证发版时脚本上传功能
- [ ] 验证脚本执行权限设置
- [ ] 验证脚本执行后服务启动

### 前端功能测试

#### 1. 服务管理页面 - 启动脚本管理入口
- [ ] 点击"更多"→"启动脚本管理"能打开对话框
- [ ] 对话框显示脚本选择下拉框
- [ ] 对话框显示版本列表表格

#### 2. 脚本选择功能
- [ ] 下拉框显示所有可用脚本
- [ ] 支持下拉过滤搜索
- [ ] 支持清空选择
- [ ] 选择脚本后自动加载版本列表

#### 3. 版本管理功能
- [ ] 版本列表显示版本号、端口、JVM 参数等信息
- [ ] 点击"激活"能成功激活版本
- [ ] 激活成功后版本列表自动刷新
- [ ] 激活的版本显示"当前激活"标识

#### 4. 脚本详情查看
- [ ] 点击"查看"能打开详情对话框
- [ ] 详情显示脚本名称、描述、端口等配置
- [ ] 详情显示完整的脚本内容

#### 5. 服务编辑表单
- [ ] 编辑服务时显示启动脚本选择框
- [ ] 选择脚本后能保存配置
- [ ] 保存后服务关联正确的脚本和版本

### 集成测试

#### 1. 端到端测试流程
1. 创建新的启动脚本
2. 编辑脚本配置
3. 在服务管理中关联脚本
4. 执行发版操作
5. 验证脚本上传到服务器
6. 验证服务通过脚本启动

## 已知问题

### 已修复的问题
- [x] 重复函数定义导致构建失败 - 已删除重复的 `handleViewScriptDetail` 和 `handleGoToScriptManagement`
- [x] API 文件路径错误 - 已修正导入路径
- [x] 缺少 API 封装文件 - 已重建 `service-start-script.js`

## 测试环境
- 前端：http://localhost:5173
- 后端：http://localhost:8080
- 数据库：SQLite (opster.db)

## 测试命令

### 后端
```bash
cd opster-backend
mvn spring-boot:run
```

### 前端
```bash
cd opster-frontend
npm run dev
```

### 数据库初始化
```bash
# 执行数据库迁移脚本
sqlite3 opster.db < db/changelog/2026-03-15-create-start-script-management.sql
```

## 验收标准

1. **功能完整性**
   - 启动脚本管理功能可在服务管理页面访问
   - 脚本选择、版本管理、详情查看功能正常
   - 服务编辑时可配置启动脚本
   - 发版时自动上传并执行脚本

2. **UI/UX**
   - 对话框布局合理，操作流畅
   - 错误提示清晰
   - 加载状态显示正确

3. **数据一致性**
   - 脚本版本关联正确
   - 激活版本后服务表同步更新
   - 删除脚本时级联处理

## 后续工作

1. **性能优化**
   - 脚本列表分页加载（如果需要）
   - 脚本内容缓存

2. **用户体验增强**
   - 脚本预览功能（编辑时实时预览）
   - 脚本模板库（预置多种模板）
   - 版本对比功能

3. **安全加固**
   - 脚本内容安全校验
   - 执行权限控制
