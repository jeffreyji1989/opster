# 服务管理优化 - 实施总结

## 变更目标
支持一个 Git 仓库地址配置多个部署服务

## 完成时间
2026-02-22

## 变更内容

### 1. 数据模型调整（AppService 实体类）
**文件**: `opster-backend/src/main/java/com/opster/module/service/entity/AppService.java`

**新增字段**:
- `serviceName`: 服务名称，用于区分同一 Git 仓库的多个部署服务
- `serviceType`: 服务类型（原 repositoryType 字段重命名）
- `sourcePath`: 源码目录，系统自动计算（公式：opster.deploy-path + projectCode + "source"）
- `compilePath`: 编译目录，用户手动填写
- `buildScript`: 编译脚本，统一 maven 命令和构建命令

**字段废弃**:
- `repositoryType` → 重命名为 `serviceType`
- `mavenCmd` 和 `buildCmd` → 统一为 `buildScript`

### 2. 数据库变更
**文件**: `db/changelog/2026-02-22-alter-service-add-multi-deployment-fields.sql`

**新增字段**:
```sql
ALTER TABLE `service` ADD COLUMN `service_name` VARCHAR(100);
ALTER TABLE `service` ADD COLUMN `service_type` INT DEFAULT 1;
ALTER TABLE `service` ADD COLUMN `source_path` VARCHAR(500);
ALTER TABLE `service` ADD COLUMN `compile_path` VARCHAR(500);
ALTER TABLE `service` ADD COLUMN `build_script` TEXT;
```

**数据迁移**:
- `repository_type` → `service_type`
- `maven_cmd` 或 `build_cmd` → `build_script`

### 3. 前端表单调整
**文件**: `opster-frontend/src/views/Service.vue`

**表单变更**:
1. 新增"服务名称"输入框
2. 新增"源码目录"（只读显示，自动计算）
3. 新增"编译目录"（可编辑）
4. "仓库类型"重命名为"服务类型"
5. "Maven 命令"和"构建命令"统一为"编译脚本"

**表格变更**:
1. 新增"服务名称"列
2. 所有条件判断从 `repositoryType` 改为 `serviceType`

**方法调整**:
- `getRepoTypeLabel()` → `getServiceTypeLabel()`
- `getRepoTypeColor()` → `getServiceTypeColor()`
- `getRepositoryTypeValue()` → `getServiceTypeValue()`
- 新增 `getComputedSourcePath()` 方法

### 4. 后端业务逻辑调整
**文件**: `opster-backend/src/main/java/com/opster/module/service/service/impl/AppServiceServiceImpl.java`

**新增功能**:
- 自动计算源码目录逻辑
- 字段兼容性处理（serviceType 兼容 repositoryType，buildScript 兼容 mavenCmd/buildCmd）

**新增依赖**:
```java
@Value("${opster.deploy-path}")
private String deployPath;
```

### 5. 新增枚举类
**文件**: `opster-backend/src/main/java/com/opster/common/enums/ServiceType.java`

- 创建 ServiceType 枚举，与 RepositoryType 保持一致
- 用于字段命名区分

## 目录计算规则

### 源码目录（共享维度）
- **计算公式**: `{opster.deploy-path}/{projectCode}/source`
- **共享规则**: 同一 Git 仓库地址的所有服务共享同一个源码目录
- **显示方式**: 只读，系统自动计算

### 编译目录（独立维度）
- **默认值**: 空，由用户手动填写
- **示例**: `target`（后端）、`dist`（前端）
- **共享规则**: 每个部署服务有独立的编译目录

## 兼容性处理

### 后端兼容
1. 保存时自动处理字段映射:
   - `serviceType` 为空时从 `repositoryType` 获取
   - `buildScript` 为空时从 `mavenCmd` 或 `buildCmd` 获取

2. 源码目录自动计算:
   - 新增服务时自动计算并填充
   - 编辑时保留原有值（除非为空）

### 前端兼容
1. 表格显示兼容新旧字段:
   - 优先使用 `serviceType`，兼容 `repositoryType`
   - 优先使用 `buildScript`，兼容 `mavenCmd` 和 `buildCmd`

2. 编辑时自动加载旧字段值

## 待执行事项

### 1. 执行数据库变更
```bash
mysql -h 39.155.134.146 -P 33306 -u <用户名> -p opster < db/changelog/2026-02-22-alter-service-add-multi-deployment-fields.sql
```

### 2. 重启后端服务
```bash
cd opster-backend
mvn spring-boot:run
```

### 3. 重启前端服务
```bash
cd opster-frontend
npm run dev
```

### 4. 功能测试
- [ ] 新增服务：验证服务名称、源码目录、编译目录、服务类型、编译脚本字段是否正常
- [ ] 编辑服务：验证字段加载和保存是否正常
- [ ] 数据兼容：验证旧数据是否能正常显示和编辑
- [ ] 源码目录计算：验证计算规则是否正确
- [ ] 列表显示：验证服务名称列是否正常显示

## 注意事项
1. 执行数据库变更前请备份数据库
2. 建议在测试环境先验证
3. 旧数据会自动迁移，无需手动处理
4. 前端和后端需要同时更新才能正常使用
