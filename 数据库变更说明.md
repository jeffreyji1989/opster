# 数据库变更说明

## 变更时间
2026-02-22

## 变更内容
支持一个 Git 地址配置多个部署服务

## SQL 文件位置
`db/changelog/2026-02-22-alter-service-add-multi-deployment-fields.sql`

## 执行方式
请连接到 MySQL 数据库执行以下 SQL 文件:

```bash
mysql -h 39.155.134.146 -P 33306 -u <用户名> -p opster < db/changelog/2026-02-22-alter-service-add-multi-deployment-fields.sql
```

或者在数据库客户端中直接执行该 SQL 文件的内容。

## 变更详情

### 1. 新增字段
- `service_name`: 服务名称，用于区分同一 Git 仓库的多个部署服务
- `service_type`: 服务类型（原 repository_type 重命名）
- `source_path`: 源码目录（系统自动计算）
- `compile_path`: 编译目录（用户手动填写）
- `build_script`: 编译脚本（统一 maven 命令和构建命令）

### 2. 数据迁移
- `repository_type` 的值会自动迁移到 `service_type`
- `maven_cmd` 或 `build_cmd` 的值会自动迁移到 `build_script`

## 注意事项
- 执行前请备份数据库
- 建议在测试环境先执行验证
