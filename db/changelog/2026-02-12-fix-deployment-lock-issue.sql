-- ================================================================
-- 修复部署锁问题
-- 日期: 2026-02-12
-- 说明: 修复并发发版时锁获取失败的问题，添加锁管理 API
-- ================================================================

-- 1. 清理可能残留的过期锁（执行前先清理）
DELETE FROM deployment_lock WHERE expire_time < datetime('now', '-1 hour');

-- 2. 添加备注说明（SQLite 不支持 COMMENT，仅作文档）
-- 锁超时时间默认 30 分钟
-- 如果发版异常退出，锁可能会残留
-- 需要通过定期任务或手动清理过期锁
