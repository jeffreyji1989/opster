我已分析了您的需求与当前系统实现的差距。目前的系统已经完成了大部分核心的 CRUD 功能，但还需要在“项目监控”、“首页统计细节”以及一些数据联动逻辑上进行完善。

以下是补充完善的实施计划：

## 1. 首页 (Dashboard) 升级
**目标**: 丰富首页显示内容，增加“服务状态”的具体统计。
- **后端**: 修改 `DashboardController`，除总数外，增加按状态分组的服务数量统计（正常、异常、未启动）。
- **前端**: 修改 `Dashboard.vue`，增加服务健康状态的统计展示（如：正常服务 xx 个，异常服务 xx 个）。

## 2. 完善服务器管理逻辑
**目标**: 自动维护“部署项目个数”字段。
- **后端**: 在服务 (AppService) 创建/删除时，自动更新对应服务器 (Server) 的 `deployedCount` 字段，确保数据准确。

## 3. 新增“项目监控”模块
**目标**: 实现“按项目+IP维度查询项目状态”的功能。
- **后端**:
    - 在 `ServiceController` 中增加高级查询接口，支持通过 `projectId` 和 `serverIp` 筛选服务实例。
    - 实现关联查询逻辑（通过 IP 查 Server ID，再查 Service）。
- **前端**:
    - 新增视图 `Monitor.vue`。
    - 实现筛选栏：选择项目（下拉）、输入 IP。
    - 展示列表：显示项目名称、服务器 IP、当前服务状态、监控地址链接等。
    - 在路由和侧边栏中添加“项目监控”入口。

## 4. 验证与测试
- 运行测试用例，确保新逻辑正确。
- 启动前后端验证所有功能模块闭环。
